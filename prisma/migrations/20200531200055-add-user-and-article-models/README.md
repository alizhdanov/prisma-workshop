# Migration `20200531200055-add-user-and-article-models`

This migration has been generated by Ali Zhdanov at 5/31/2020, 8:00:55 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "workshop"."Test" (
"id" SERIAL,"name" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "workshop"."User" (
"bio" text   ,"email" text  NOT NULL ,"id" SERIAL,"image" text   ,"username" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "workshop"."Article" (
"authorId" integer  NOT NULL ,"body" text  NOT NULL ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"title" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "Test.name" ON "workshop"."Test"("name")

CREATE UNIQUE INDEX "User.username" ON "workshop"."User"("username")

CREATE UNIQUE INDEX "User.email" ON "workshop"."User"("email")

ALTER TABLE "workshop"."Article" ADD FOREIGN KEY ("authorId")REFERENCES "workshop"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200528093122-init..20200531200055-add-user-and-article-models
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator prisma_client {
   provider = "prisma-client-js"
@@ -10,4 +10,23 @@
 model Test {
   id   Int    @id @default(autoincrement())
   name String @unique
 }
+
+model User {
+  id       Int       @id @default(autoincrement())
+  username String    @unique
+  email    String    @unique
+  bio      String?
+  image    String?
+  articles Article[]
+}
+
+model Article {
+  id        Int      @id @default(autoincrement())
+  title     String
+  body      String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  authorId  Int
+  author    User     @relation(fields: [authorId], references: [id])
+}
```


